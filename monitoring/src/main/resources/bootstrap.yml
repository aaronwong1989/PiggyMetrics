spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: passwd
#     password: ${CONFIG_SERVICE_PASSWORD}
      username: user